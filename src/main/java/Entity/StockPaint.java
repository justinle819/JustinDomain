package Entity;

import ValueObject.PigmentColor;

public class StockPaint extends Paint{
    public StockPaint(double volume, PigmentColor pigmentColor) {
        super(volume, pigmentColor);
    }
}
